import { readAllData as readDataSize } from "@/api/size";
import { readAllData as readDataLocation } from "@/api/location";
import { addData as addList, readAllData as readList } from "@/api/list";

export {
  readDataSize,
  readDataLocation,
  addList,
  readList,
}
